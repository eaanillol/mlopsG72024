apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: miapi-claim0
  name: miapi-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
